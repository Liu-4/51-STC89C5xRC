#ifndef __INT_KEY_H__
#define __INT_KEY_H__

/**
 * @brief 检测按键1是否按下
 * 
 * @return bit 按下为1，未按下为0
 */
bit Int_key_IsSw1Preassed();

/**
 * @brief 检测按键2是否按下
 * 
 * @return bit 按下为1，未按下为0
 */
bit Int_key_IsSw2Preassed();

/**
 * @brief 检测按键3是否按下
 * 
 * @return bit 按下为1，未按下为0
 */
bit Int_key_IsSw3Preassed();

/**
 * @brief 检测按键4是否按下
 * 
 * @return bit 按下为1，未按下为0
 */
bit Int_key_IsSw4Preassed();


#endif /* __INT_KEY_H__ */